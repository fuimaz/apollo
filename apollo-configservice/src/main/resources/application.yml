spring:
  application:
    name: apollo-configservice
  profiles:
    active: ${apollo_profile}
  datasource:
    url: jdbc:mysql://localhost:3306/ApolloConfigDB?useSSL=false&characterEncoding=utf8
    username: root
    password: fuimazv8p

ctrip:
  appid: 100003171

server:
  port: 8080

logging:
  file: logs/100003171/apollo-configservice.log
